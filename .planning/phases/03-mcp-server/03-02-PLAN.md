---
wave: 2
depends_on: [03-01-PLAN.md]
files_modified:
  - C:/Users/razva/.gemini/config/mcp_config.json
  - C:/Users/razva/.gemini/antigravity-ide/mcp_config.json
autonomous: true
requirements_addressed:
  - MCP-04
  - MCP-05
---

# 03-02: MCP Registration

## Objective
Register the new MCP server in the global Antigravity configurations.

## Tasks

```xml
<task>
  <action>Add the `site-audit` entry to `C:/Users/razva/.gemini/config/mcp_config.json`. The command should be `node` and the arg should be the absolute path to `mcp/index.js`.</action>
  <read_first>
    - C:/Users/razva/.gemini/config/mcp_config.json
  </read_first>
  <acceptance_criteria>
    - mcp_config.json contains "site-audit" key
    - The path points to the correct Site-test-tool directory
  </acceptance_criteria>
</task>

<task>
  <action>Add the `site-audit` entry to `C:/Users/razva/.gemini/antigravity-ide/mcp_config.json` (if it exists) using the same structure.</action>
  <read_first>
    - C:/Users/razva/.gemini/antigravity-ide/mcp_config.json
  </read_first>
  <acceptance_criteria>
    - antigravity-ide/mcp_config.json contains "site-audit" key
  </acceptance_criteria>
</task>
```

## Verification
- MCP server connects correctly when starting a new chat session.
