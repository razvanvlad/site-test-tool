---
wave: 1
depends_on: []
files_modified:
  - package.json
  - audit.js
  - README.md
autonomous: true
requirements_addressed:
  - SKILL-02
---

# 04-01: CLI Polish and Documentation

## Objective
Add a premium feel to the `audit.js` CLI output using `chalk` and `ora`. Write a comprehensive `README.md` that acts as the source of truth for the project's architecture, setup, and extensibility.

## Tasks

```xml
<task>
  <action>Install `chalk` and `ora`: `npm install chalk ora`</action>
  <read_first>
    - package.json
  </read_first>
  <acceptance_criteria>
    - package.json contains `chalk` and `ora` in dependencies
  </acceptance_criteria>
</task>

<task>
  <action>Update `audit.js` to import `chalk` and `ora`. Replace `console.log` progress messages with `ora` spinners (e.g. `const spinner = ora('Running Playwright...').start()`, then `spinner.succeed()`). Use `chalk` to colorize the summary output at the end (e.g., `chalk.red.bold('Critical:')`). Add `console.table(severityCounts)` or similar formatted layout.</action>
  <read_first>
    - audit.js
  </read_first>
  <acceptance_criteria>
    - audit.js imports `ora` and `chalk`
    - audit.js uses `ora().start()` for the 4 engine steps
    - audit.js uses `chalk` colors for severity counts
  </acceptance_criteria>
</task>

<task>
  <action>Write `README.md`. Include Project Overview, Architecture (CLI + Express Dashboard + MCP Server), Installation Instructions, Usage (CLI commands), Database Schema overview, and How to Add New Engines.</action>
  <read_first>
    - README.md (if exists)
    - db/schema.sql
  </read_first>
  <acceptance_criteria>
    - README.md exists and contains "Project Overview"
    - README.md contains "Architecture"
    - README.md contains "Installation"
    - README.md contains "Database Schema"
  </acceptance_criteria>
</task>
```

## Verification
- Run `node audit.js https://example.com` and verify spinners appear and output is colorized.
- README.md is thorough and correctly formatted.
