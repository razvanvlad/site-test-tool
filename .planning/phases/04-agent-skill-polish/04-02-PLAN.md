---
wave: 2
depends_on: []
files_modified:
  - C:/Users/razva/.gemini/config/skills/site-audit-reporter/SKILL.md
  - .agents/skills/site-audit-reporter/SKILL.md
autonomous: true
requirements_addressed:
  - SKILL-01
  - SKILL-03
---

# 04-02: Agent Skill

## Objective
Package the workflow into an Antigravity SKILL.md. This skill will teach agents how to audit sites and fetch findings using the MCP server tools, and instruct them to only audit and not autonomously fix/verify without user request.

## Tasks

```xml
<task>
  <action>Create `C:/Users/razva/.gemini/config/skills/site-audit-reporter/SKILL.md` (creating the directory if needed). Add YAML frontmatter with `name: site-audit-reporter` and `description: You are a site auditor. Run audits and report findings.`. Provide explicit guidelines: 1. When asked to QA/audit a site, run `mcp_site-audit_run_audit` (or the equivalent registered tool) on the URL. 2. Fetch the findings using `mcp_site-audit_get_findings` with `format: 'markdown'`. 3. Present the findings to the user clearly. 4. NEVER invent findings. 5. Flag 403/999 status codes as "needs manual check". 6. Ask the user if they want to fix anything. Wait for user request before fixing or using `compare_screenshots`.</action>
  <read_first>
    - .planning/ROADMAP.md
  </read_first>
  <acceptance_criteria>
    - The skill file exists at the global path
    - The skill file contains frontmatter
    - The skill file explicitly mentions the `mcp_site-audit` tools
  </acceptance_criteria>
</task>

<task>
  <action>Copy the global skill file to `.agents/skills/site-audit-reporter/SKILL.md` in the project root to keep a project-local version tracked in Git.</action>
  <read_first>
    - C:/Users/razva/.gemini/config/skills/site-audit-reporter/SKILL.md
  </read_first>
  <acceptance_criteria>
    - .agents/skills/site-audit-reporter/SKILL.md exists and matches the global file
  </acceptance_criteria>
</task>
```

## Verification
- Starting a fresh chat and asking to "QA klap.ro" triggers the skill, successfully invoking the MCP server and presenting the markdown findings.
